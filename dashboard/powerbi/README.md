# Power BI Executive Dashboard

This directory is reserved for Power BI workbook templates (`.pbit` / `.pbix`) and report theme definitions.

- **Status:** Planned / placeholder (interactive decision-support interface is actively served via Streamlit in `dashboard/streamlit/`).
- **Data Marts:** Analytical serving datasets and SQL star-schema views formatted for Power BI ingestion are available in `sql/04_power_bi_corridor_history_mart.sql` and `data/processed/power_bi_*.parquet`.
- **Note:** Local `.pbix` binary files are excluded from git tracking via `.gitignore`.
