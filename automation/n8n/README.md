# n8n Workflow Automation

This directory is reserved for exported n8n workflow definitions and webhook integration specifications.

- **Status:** Planned / placeholder (pipeline currently executed via local CLI and PowerShell automation in `automation/verify_project.ps1`).
- **Data Marts:** Analytical serving views and SQL transformation pipelines are available under `sql/`.
